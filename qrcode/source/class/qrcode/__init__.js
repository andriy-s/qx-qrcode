/**
 * This is the top-level namespace of the QR code generator library.
 *
 * There are several classes in this namespace. The end-users of the library
 * should mainly be concerned with {@link qrcode.QRCode} class. The rest of the
 * classes are only internally used by the {@link qrcode.QRCode} class and
 * should never be used directly.
 */
